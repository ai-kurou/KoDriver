package kurou.kodriver.feature.acewindowsnarrator

import org.koin.dsl.module

/**
 * ACE (Assetto Corsa EVO) Windows版 WAV音声アナウンス（ace-windows-narrator feature）の Koin モジュール。
 *
 * 現時点ではモジュールの雛形のみで中身は未実装。ViewModel・SoundPlayer・WAVリソースの追加は別 PR で行う。
 */
val aceWindowsNarratorModule = module { }
