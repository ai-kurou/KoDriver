package kurou.kodriver.domain.model

/** ACE 車両接近アナウンスの、接近判定に使う距離しきい値のデフォルト値。単位は meters。 */
const val ACE_WINDOWS_VEHICLE_APPROACH_THRESHOLD_METERS_DEFAULT = 10.0

/** ACE 車両接近アナウンスの、接近開始時読み上げの有効/無効デフォルト値。 */
const val ACE_WINDOWS_VEHICLE_APPROACH_START_READOUT_ENABLED_DEFAULT = true
